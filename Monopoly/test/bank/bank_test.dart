import "package:test/test.dart";
import "package:monopoly/src/components/bank/bank.dart";

void main() {
  setUp(() async {
    // Bank bank = new Bank();
  });
  group("some group", () {
    test("some asyncronous test", () async {
      bool someBool = true;
      expect(someBool, isTrue);
    });
  });
}
