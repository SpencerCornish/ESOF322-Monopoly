import "package:test/test.dart";
import "package:monopoly/src/components/jail/jail.dart";

void main() {
  setUp(() async {
// Jail jail = new Jail();
  });
  group("some group", () {
    test("some asyncronous test", () async {
      bool someBool = true;
      expect(someBool, isTrue);
    });
  });
}
